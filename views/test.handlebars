<div>
<h2>Registration Test</h2>
<p>Fill out the form to register a new user to the database.</p>
<hr>
<form id="newUser">
   <div class="form-group">      
      <label for="firstName">First Name:</label>
      <input type="text" name="firstName" id="firstName">
   </div>
   <div class="form-group">
      <label for="lastName">Last Name:</label>
      <input type="text" name="lastName" id="lastName">
   </div>
   <div class="form-group">
      <label for="username">Username:</label>
      <input type="text" name="username" id="username">
   </div>
   <div class="form-group">
      <label for="password">Password:</label>
      <input type="text" name="password" id="password">
   </div>
   <button type="submit">Submit</button>
   <br>
   <br>
   <div>
      <em><p id="newUserMessage"></p></em>
   </div>
</form>
<br>
<hr>

<script>
   $("#newUser").on("submit", function(event) {
      event.preventDefault();

      var newUser = {
         firstName: $("#firstName").val().trim(),
         lastName: $("#lastName").val().trim(),
         username: $("#username").val().trim(),
         password: $("#password").val().trim()
      }

      $.post("/registration", newUser).then(function(res) {
         $("#newUserMessage").text(res);
      })
   })
</script>
</div>

<div>
<h2>New Snippet Test</h2>
<p>Fill out the form to create a new snippet.</p>
<hr>
<form id="newSnippet">
   <div class="form-group">      
      <label for="snippet">Snippet:</label>
      <input type="text" name="snippet" id="snippet">
   </div>
   <div class="form-group">
      <label for="language">Langauge (e.g. "Javascript"):</label>
      <input type="text" name="language" id="language">
   </div>
   <div class="form-group">
      <label for="tags">Tags:</label>
      <input type="text" name="tags" id="tags">
   </div>
   <div class="form-group">
      <label for="description">Description:</label>
      <input type="text" name="description" id="description">
   </div>
   <div class="form-group">
      <label for="authorID">Author ID:</label>
      <input type="number" name="authorID" id="authorID">
   </div>
   <button type="submit">Submit</button>
   <br>
   <br>
   <div>
      <em><p id="newSnippetMessage"></p></em>
   </div>
</form>
<br>
<hr>

<script>
   $("#newSnippet").on("submit", function(event) {
      event.preventDefault();

      var newSnippet = {
         snippet: $("#snippet").val().trim(),
         language: $("#language").val().trim(),
         tags: $("#tags").val().trim(),
         description: $("#description").val().trim(),
         authorID: $("#authorID").val()
      }

      $.post("/dashboard", newSnippet).then(function(res) {
         $("#newSnippetMessage").text(res);
      })
   })
</script>
</div>

<div>
<h2>Update Snippet Test</h2>
<p>Fill out the form to update a new snippet. Blank fields will result in empty columns in the row.</p>
<hr>
<form id="updateSnippet">
   <div class="form-group">      
      <label for="id">ID of the snippet you want to update:</label>
      <input type="number" name="id" id="id">
   </div>
   <div class="form-group">      
      <label for="updateSnippet">Snippet:</label>
      <input type="text" name="updateSnippet" id="updateSnippetInput">
   </div>
   <div class="form-group">
      <label for="updateLanguage">Langauge (e.g. "Javascript"):</label>
      <input type="text" name="updateLanguage" id="updateLanguage">
   </div>
   <div class="form-group">
      <label for="updateTags">Tags:</label>
      <input type="text" name="updateTags" id="updateTags">
   </div>
   <div class="form-group">
      <label for="updateDescription">Description:</label>
      <input type="text" name="updateDescription" id="updateDescription">
   </div>
   <button type="submit">Submit</button>
   <br>
   <br>
   <div>
      <em><p id="updateSnippetMessage"></p></em>
   </div>
</form>
<br>
<hr>

<script>
   $("#updateSnippet").on("submit", function(event) {
      event.preventDefault();

      var updateSnippet = {
         id: $("#id").val(),
         snippet: $("#updateSnippetInput").val().trim(),
         language: $("#updateLanguage").val().trim(),
         tags: $("#updateTags").val().trim(),
         description: $("#updateDescription").val().trim(),
      }

      $.ajax({
         method: "PUT",
         url: "/dashboard",
         data: updateSnippet
      }).then(function(res){
         $("#updateSnippetMessage").text(res);
      });
   })
</script>
</div>